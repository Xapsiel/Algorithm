from scipy.optimize import linprog

# Коэффициенты целевой функции (нужно минимизировать)
c = [-2/3, -1/3, 0, 0, 0]

# Коэффициенты ограничений (левая часть)
A_eq = [
    [1, 1, 1, 1, 0],  # X1 + X2 + X3 + X4 = 500
    [4/3, -4/3, -3, 0, 1]  # 4/3*X1 - 4/3*X2 - 3*X3 + X5 = 0
]

# Правая часть ограничений
b_eq = [500, 0]

# Ограничения неотрицательности переменных
bounds = [(0, None), (0, None), (0, None), (0, None), (0, None)]

# Решение задачи
res = linprog(c, A_eq=A_eq, b_eq=b_eq, bounds=bounds, method='simplex')

# Вывод результата
print("Статус решения:", res.message)
print("Максимальное значение целевой функции:", -res.fun)  # так как минимизировали отрицательное значение
print("Оптимальные значения переменных:")
print("X1 =", res.x[0])
print("X2 =", res.x[1])
print("X3 =", res.x[2])
print("X4 =", res.x[3])
print("X5 =", res.x[4])
